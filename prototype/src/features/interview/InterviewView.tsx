import { ArrowRight, CalendarDays, CheckCircle2, Sparkles } from "lucide-react";
import type { DemoState } from "../../domain/model";
import { ObjectHeader, PrimaryButton, ReturnToday, StatusPill } from "../../components/ui";

export function InterviewView({ state, dispatch }: { state: DemoState; dispatch: (action: any) => void }) {
  return <div className="feature-view"><ReturnToday onClick={() => dispatch({ type: "set-view", view: "today" })} /><ObjectHeader eyebrow="关联事件 · 周五 14:00" title={<>把一次面试，<br /><em>变成下一次的底气。</em></>} description="准备、进行、记录反馈，再把真实结果带回当前机会和职业事实。" status={<StatusPill tone="info">待准备</StatusPill>}><div className="object-links"><button type="button" onClick={() => dispatch({ type: "set-view", view: "opportunity" })}><CalendarDays size={14} />回到当前机会 <ArrowRight size={13} /></button></div></ObjectHeader><div className="interview-layout"><section className="interview-event"><div className="event-summary"><span className="calendar-icon"><CalendarDays size={19} /></span><div><p className="eyebrow">澄屿科技 · 技术负责人初面</p><h2>周五 14:00</h2><p>这次面试要验证：岗位是否真的有架构决策空间。</p></div></div><div className="interview-sections"><div><span>AI 已准备</span><strong>3 个经历切入点 · 5 个反问</strong></div><div><span>面试后记录</span><strong>问题、回答、对方反馈和你的判断</strong></div></div><PrimaryButton onClick={() => dispatch({ type: "set-life-stage", stage: "outcome" })}>记录面试结果</PrimaryButton></section><aside className="interview-aside"><div className="interview-principle"><Sparkles size={17} /><h3>这不是成败评分</h3><p>系统只帮你保留事件和证据。面试是否值得继续，仍由你结合现场信息判断。</p></div><div className="interview-timeline"><h3>这次事件留下的关系</h3><div><CheckCircle2 size={14} /><span>验证团队授权范围</span></div><div><CheckCircle2 size={14} /><span>补充岗位实际工作方式</span></div><div><CheckCircle2 size={14} /><span>反馈给下一次职业策略</span></div></div></aside></div></div>;
}

