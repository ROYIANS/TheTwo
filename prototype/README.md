# 择途 React 产品原型

这是择途完整求职决策主链的工程化前端原型。它用于验证产品结构、界面和交互，不代表正式前端、后端或部署技术选型。

## 启动

```powershell
corepack pnpm install
corepack pnpm dev
```

默认地址：`http://127.0.0.1:4173`

## 边界

* 所有数据均为虚构示例；
* 状态只存在于浏览器内存；
* 不调用网络、模型、数据库或第三方招聘平台；
* 不会自动发送消息或提交申请。
